## ⚙️ Configuración inicial

1. Crear `.env` en la carpeta raíz del proyecto:
```env
    POSTGRES_PORT=5434
    API_PORT=8001
```

2. Crear `.env` en `backend/` basándose en `backend/.env.example`

3. Crear `.env` en `frontend/` basándose en `frontend/.env.example`